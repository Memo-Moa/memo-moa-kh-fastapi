services:
  memo-moa:
    build: .
    image: memo-moa-kh-fastapi:latest
    container_name: memo-moa-kh-fastapi
    restart: unless-stopped
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      - PORT=${PORT:-8000}
